import { LoggingInteceptorInterceptor } from './logging-inteceptor.interceptor';

describe('LoggingInteceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new LoggingInteceptorInterceptor()).toBeDefined();
  });
});
